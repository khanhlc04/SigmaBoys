# Force use pre-built binary wheels (no compilation needed)
--prefer-binary

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.1
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# OpenAI & LangChain
openai==1.3.8

# tiktoken - use version with pre-built wheels for Linux
tiktoken==0.5.1

# LangChain - use newer versions with better compatibility
langchain-core==0.1.10
langchain-openai==0.0.5
langchain-community==0.0.10
langchain-text-splitters==0.0.1

# MongoDB
motor==3.3.2
pymongo==4.6.0